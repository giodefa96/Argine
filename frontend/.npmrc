# Frontend supply-chain hardening (pnpm).
# pnpm blocks dependency install/build scripts BY DEFAULT — the packages allowed
# to run them are explicitly allowlisted in pnpm-workspace.yaml -> onlyBuiltDependencies.

# Pin exact versions on `pnpm add` — no "^" ranges, install exactly what was reviewed.
save-exact=true
fund=false
